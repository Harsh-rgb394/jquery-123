  // int sum = 0;
    // for (auto it = st.begin(); it != st.end(); it++)
    // {
    //     sum += *it;
    // }
    // int m = n - 1;
    // int sum1 = (m) * (m + 1) / 2;
    // ans.push_back((sum1 - sum));